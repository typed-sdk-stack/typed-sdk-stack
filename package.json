{
    "$schema": "https://www.schemastore.org/package.json",
    "name": "typed-sdk-stack",
    "description": "TypeScript-first, strongly-typed SDKs and shared tooling for building reliable API clients on top of RapidAPI.",
    "private": true,
    "scripts": {
        "build": "turbo run build",
        "check-types": "turbo run check-types",
        "test": "turbo run test",
        "lint": "turbo run lint",
        "lint:fix": "turbo run lint:fix",
        "validate": "turbo run validate",
        "prepare": "lefthook install",
        "clean": "rimraf build dist node_modules .turbo tsconfig.tsbuildinfo apps/*/build apps/*/dist apps/*/node_modules apps/*/.turbo apps/*/tsconfig.tsbuildinfo packages/*/build packages/*/dist packages/*/node_modules packages/*/.turbo packages/*/tsconfig.tsbuildinfo"
    },
    "packageManager": "bun@1.3.0",
    "engines": {
        "node": ">=22",
        "bun": ">=1.3"
    },
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "@biomejs/biome": "^2.3.11",
        "@commitlint/cli": "^20.3.0",
        "@commitlint/config-conventional": "^20.3.0",
        "@types/bun": "^1.3.5",
        "lefthook": "^2.0.13",
        "rimraf": "^6.1.2",
        "turbo": "^2.7.2",
        "typescript": "^5.9.3"
    }
}
